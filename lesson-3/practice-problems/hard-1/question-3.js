//---------------------------------Part A---------------------------------------
/*
function messWithVars(one, two, three) {
  one = two;
  two = three;
  three = one;
}

let one = ["one"];
let two = ["two"];
let three = ["three"];

messWithVars(one, two, three);

console.log(`one is: ${one}`);
console.log(`two is: ${two}`);
console.log(`three is: ${three}`);

// No change will be made to the vaiables declared outside of the function's
// scope. The function parameters shadow the global vaiables.
*/

//---------------------------------Part B---------------------------------------
/*
function messWithVars(one, two, three) {
  one = ["two"];
  two = ["three"];
  three = ["one"];
}

let one = ["one"];
let two = ["two"];
let three = ["three"];

messWithVars(one, two, three);

console.log(`one is: ${one}`);
console.log(`two is: ${two}`);
console.log(`three is: ${three}`);

// No change will be made to the vaiables declared outside of the function's
// scope. The function parameters shadow the global variables.
*/
//---------------------------------Part C---------------------------------------
/*
function messWithVars(one, two, three) {
  one.splice(0, 1, "two");
  two.splice(0, 1, "three");
  three.splice(0, 1, "one");
}

let one = ["one"];
let two = ["two"];
let three = ["three"];

messWithVars(one, two, three);

console.log(`one is: ${one}`);
console.log(`two is: ${two}`);
console.log(`three is: ${three}`);

Splice is a mutating method and since both the local and global vaiables both
point to the same object any changes to the object will effect the original object.
*/
